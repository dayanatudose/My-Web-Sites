import React from 'react';
import { User } from '../../models/user';

interface Props {
    users: User[];
}

export default function UserList({users}: Props) {
    return(
        <div>
            
        </div>
    )
}